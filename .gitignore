_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
# Ignore folders generated by Bundler (but not top-level vendor files like README)
.bundle/
vendor/bundle/

# Do NOT ignore Gemfile.lock — keep the lockfile under source control
# Gemfile.lock
